import Head from 'next/head'

const Home: React.FC = () => {
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        <h1>Boilerplate NextJS/SWR/StyledComponents/Axios 🖖</h1>
      </main>
    </div>

  )
}

export default Home